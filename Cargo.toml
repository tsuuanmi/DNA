[package]
name = "dna"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "DNA sequencing trace analysis in Rust"
repository = "https://github.com/tsuuanmi/DNA"
readme = "README.md"
publish = false

[features]
default = []
fuzzing = []

[dependencies]
chrono = { version = "0.4.45", default-features = false, features = ["clock"] }
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"
toml = "1.1"

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
tempfile = "3"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
expect_used = "deny"
unwrap_used = "deny"
