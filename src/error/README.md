# Error

Owns the typed `Error` boundary and shared `Result<T>` alias.

Errors preserve structured failure context without printing, exiting, or deciding
recovery policy.

See [system architecture](../../docs/architecture/overview.md) and
[quality requirements](../../docs/requirements/quality-validation.md).
